import { Pressable, StyleSheet, Text } from "react-native";

export const FooterMenu = ({ onPress, title, icon }) => {
    return (
    )
} 

const styles = StyleSheet.create({

}); 
//falta fazer -- yas estudando conteúdo